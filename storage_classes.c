#include <stdio.h>

int myfunc(int a,int b)
{
    int sum; //or auto int sum;
    sum=a+b;
    return sum;
}

int main()
{
    // declaration - telling the compiler about the variable (no space reserved)
    // defination - declaration + space reservation

    return 0;
}